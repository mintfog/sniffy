---
title: HTTPS and the root certificate
description: Install and trust the Sniffy root certificate, scope decryption, and handle pinning and outbound verification failures.
---

import Screenshot from "@/components/docs/Screenshot.astro";

Sniffy inspects HTTPS traffic using a man-in-the-middle (MITM) proxy. The client establishes a TLS connection to Sniffy, which opens a separate TLS connection to the destination server. The client must trust Sniffy’s root certificate to accept the certificates it signs for destination hosts.

Before using HTTPS decryption:

- Install and trust the root certificate on the clients you want to debug. Clients that verify certificates reject intercepted connections if they do not trust it.
- The root certificate's private key stays on your machine (`certificates/` in the configuration directory, mode `0700`). Anyone who obtains it can issue certificates for any domain that your machine will trust. **Don't share it.**

## Installing the root certificate

<Screenshot src="certs-overview.png" alt="The Certificates screen: root certificate status, download button, and platform switcher" caption="The Certificates screen" />

On first launch Sniffy generates `certificates/sniffy-ca.crt` and `sniffy-ca.key` in the configuration directory. Open the **Certificates** screen and follow the guide for your platform.

### macOS

Click "Install to This Machine" and confirm the system authorization prompt. The certificate goes into **your login keychain** and is marked as a trusted root.

The trust setting applies to the current user. On a shared machine, install the certificate separately for each user who needs it.

Manual install works too: download the `.crt`, double-click to import it into Keychain Access, find Sniffy Root CA, open it, expand "Trust" and set "When using this certificate" to "Always Trust", then close the window and confirm with your password.

### Windows

Click "Install to This Machine" and approve the UAC prompt. The certificate goes into the local machine's "Trusted Root Certification Authorities" store.

Manual install: download the `.crt`, double-click it - Install Certificate - store location "Local Machine" - place it in "Trusted Root Certification Authorities" - Finish. Restart your browser afterwards.

### Linux

Installing escalates through `pkexec` to copy the certificate into your distribution's CA anchor directory and rebuild the trust store:

| Distribution            | Anchor directory                            | Rebuild command           |
| ----------------------- | ------------------------------------------- | ------------------------- |
| Debian / Ubuntu         | `/usr/local/share/ca-certificates`          | `update-ca-certificates`  |
| RHEL / Fedora / CentOS  | `/etc/pki/ca-trust/source/anchors`          | `update-ca-trust extract` |
| Arch / Manjaro          | `/etc/ca-certificates/trust-source/anchors` | `update-ca-trust extract` |
| openSUSE                | `/etc/pki/trust/anchors`                    | `update-ca-certificates`  |

Some Linux browsers use separate NSS certificate stores. Installation attempts to update `~/.pki/nssdb` and `cert9.db` in each Firefox profile. If `certutil` is missing (`libnss3-tools` on Debian-based systems) or a database is locked, this step may be skipped. Import the certificate manually in the browser settings if needed.

If `pkexec` is unavailable, install manually using the table above. On other distributions, add the Sniffy root using whatever certificate management tools they provide.

### iOS / Android

The device needs to be on the same network as your machine and have its Wi-Fi proxy pointed at your LAN address. The Certificates screen has step-by-step guides for both systems, including a QR code for the iOS profile download. Full walkthrough in [Phones and remote clients](/en/docs/mobile/).

## Scoping decryption

<Screenshot src="settings-decrypt-scope.png" alt="HTTPS decryption in Settings: the scope selector and the allowlist field" caption="HTTPS decryption scope" />

By default every host is decrypted. Under Settings - HTTPS decryption you can narrow that; hosts outside the scope pass through as CONNECT tunnels — you still see the destination host and byte counts, but not the content.

| Scope              | Behavior                                      |
| ------------------ | --------------------------------------------- |
| All hosts          | Decrypt everything                            |
| Allowlist only     | Decrypt only listed hosts, tunnel the rest    |
| Everything but the denylist | Tunnel listed hosts, decrypt the rest |

One host per line, `*` wildcards supported: `*.example.com` matches both the bare domain `example.com` and its subdomains.

Narrowing the scope is the first thing to reach for in several situations:

- For apps using **certificate pinning**, add the destination domain to the decryption denylist to forward it through a tunnel.
- Banking, payment, or corporate SSO traffic you'd rather not have in plaintext in a capture log.
- Use an allowlist when you only need to decrypt specific domains.

Turning off "Enable HTTPS MITM" disables decryption entirely; every HTTPS connection becomes a tunnel.

## Importing and exporting the root certificate

**Export** carries the same root certificate to another device or tool. The Certificates screen offers PEM, CRT, DER, and PKCS#12. PKCS#12 contains the private key and requires a password.

**Import** configures Sniffy to use an existing CA, such as a shared test CA. PEM bundles and password-protected PKCS#12 files are supported. The imported CA takes effect without restarting; clients must trust it.

**Regenerate** replaces the root certificate and private key used by Sniffy. Install and trust the new certificate on each client, remove trust in the old certificate, and restart browsers. Regeneration does not automatically revoke trust in the old certificate on client devices.

## Server certificates for specific domains

Import a domain certificate and its private key under Server certificates. Sniffy presents it to clients connecting to matching hosts. Clients must trust its issuer chain, and the certificate must pass hostname, validity, and other verification checks.

Matching hosts are extracted from the certificate’s SAN, falling back to CN when SAN is absent. Certificates and private keys are stored in `servercerts.json` in the configuration directory.

## Outbound certificate verification

When connecting to an upstream server, Sniffy verifies the certificate chain, hostname, and validity period by default. Verification failures stop forwarding and are recorded as errors.

A client trusting Sniffy's root only means it accepts certificates Sniffy signs. It says nothing about Sniffy accepting arbitrary origin certificates.

For self-signed certificates in an internal test environment, import the origin CA into the trust store of the system running Sniffy and restart. If you need to skip verification temporarily, limit the exception to specific hosts in `config.json` in the configuration directory (see [Configuration reference](/en/docs/configuration/) for its location):

```json
{
  "tlsInsecureHosts": ["dev.example.test", "127.0.0.1"]
}
```

The list is empty by default. Entries must be exact hostnames or IPs — no ports, URLs, or wildcards — and subdomains are not matched. Quit Sniffy before editing the file by hand.

To change the list while Sniffy is running, use the management API, see [Management API reference](/en/docs/api/config/). Changes apply to subsequent requests; established long-lived connections (WSS, for instance) need to reconnect.

## Removing the root certificate

When you no longer use Sniffy, remove its root certificate from your devices’ trust stores and delete the locally stored private key.

- **macOS** — open Keychain Access, find Sniffy Root CA in the login keychain, delete it.
- **Windows** — run `certmgr.msc` (or `certlm.msc` for the local machine store) and delete Sniffy Root CA under Trusted Root Certification Authorities - Certificates.
- **Linux** — delete `sniffy-ca.crt` from the anchor directory and re-run your distribution's rebuild command. Browser NSS databases need separate removal from the browser's certificate settings.
- **iOS** — Settings - General - VPN & Device Management, remove the profile.
- **Android** — Settings - Security - Encryption & credentials - User credentials, remove it.

Then delete the `certificates/` directory from the configuration directory.

## Related

- [Troubleshooting](/en/docs/troubleshooting/) — what to check when decryption still fails
- [Phones and remote clients](/en/docs/mobile/) — installing the certificate on a phone
- [Configuration reference](/en/docs/configuration/) — `decryptScope`, `decryptAllow`, `tlsInsecureHosts`
